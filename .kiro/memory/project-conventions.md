# Convenciones del proyecto

## Package Manager

- **Solo se usa `yarn`** como package manager. No usar `npm` para instalar dependencias ni ejecutar scripts.
- Comandos: `yarn add`, `yarn remove`, `yarn test`, `yarn lint`, `yarn ios`, `yarn android`.
- No escribir comentarios inline